syntax="proto3";

package amassa;

// frontend and inputhandler
message Request {
    string email = 1;
    string  name = 2;
    string extension = 3;
    string bytes = 4;
}

message Response {}

service InputHandlerService {
    rpc RequestAction(Request) returns (Response) {}
}
